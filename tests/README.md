# tests/

Tests for AfrekaOS Offline. **Not implemented yet.**

## Planned

- Offline-behavior tests: assert no network calls are made during judged
  runtime.
- Retrieval tests: assert SQLite FTS5 returns relevant context for the two test
  prompts.
- Prompt smoke tests: assert the local model loads and responds to the two
  canonical prompts in `../metadata.json`.

Tests will be added alongside the features in later tasks.
